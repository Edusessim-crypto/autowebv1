# Leave empty for embedded PostgreSQL in local development.
DATABASE_URL=
APP_ORIGIN=http://127.0.0.1:3107
LOCAL_DATA_DIR=.data
STORAGE_PROVIDER=local
# Production requires PostgreSQL and an S3/R2 StorageProvider implementation.
